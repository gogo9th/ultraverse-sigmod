extends layout

block content
  h1= title
  form(name='delete-bill', method='post')
    div.input
      span.label session_id
      input(type='text', name='session_id', value="#{session_id}", readonly='readonly')
    div.input
      span.label bill_id
      select(name='bill_id')
        option(value='') Select Section
        each id in id_list
          option(value=id) #{id}
    div.actions
      input(type='submit', value='Submit')